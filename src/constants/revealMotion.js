/** Curtain / zoom reveal — shared by Overlay preview and Hero */

export const curtainEase = [0.4, 0, 0.2, 1]

export const CURTAIN_DURATION = 1.15

export const EXPAND_AFTER_MS = 1400

/** Transform-origin for letter + hero zoom (center of invite content) */
export const REVEAL_ORIGIN = '50% 42%'

/** Scale: envelope window → full hero */
export const REVEAL_SCALE_RANGE = [0.56, 1]

/** Lift invite content as curtains part (px) */
export const REVEAL_Y_RANGE = [18, 0]
